\documentclass[%
	pdftex,
	a4paper,
	oneside,
	12pt,
]{article}

\usepackage{amsmath,amsfonts,amssymb,amsthm}	% Einbinden der Mathematik-Pakete
\usepackage{graphics}													% Einbinden des Grafik-Pakets
\usepackage{hyperref}													% Einbinden von Internet-Addressen
\usepackage[utf8]{inputenc}										% Wiedergabe von Sonderzeichen und Umlauten
\usepackage[ngerman]{babel}
\usepackage{eurosym}													% mit \EUR{Betrag} können jetzt Eurobeträge gemacht werden
\usepackage{setspace}
\usepackage{csquotes}

\usepackage{comment}

\usepackage[style=authoryear-ibid,maxnames=4,backend=biber]{biblatex}
\defbibheading{bibintoc}

\renewcommand*{\mkbibnamefamily}[1]{\textit{#1}}
\renewcommand*{\mkbibnamegiven}[1]{\textit{#1}}
\renewcommand*{\mkbibnameprefix}[1]{\textit{#1}}
\renewcommand*{\mkbibnamesuffix}[1]{\textit{#1}}
\renewcommand*{\multinamedelim}{\addslash}
\renewcommand*{\finalnamedelim}{\addslash}
\renewcommand*{\multilistdelim}{\addslash}
\renewcommand*{\finallistdelim}{\addslash}
\DeclareFieldFormat*{title}{#1}

\renewbibmacro*{cite:labelyear+extrayear}{%
\iffieldundef{labelyear}
  {}
  {\printtext[bibhyperref]{%
   \printtext[parens]{%
   \printfield{labelyear}%
   \printfield{extrayear}}}}}

\makeatletter
\newcommand{\MSonehalfspacing}{%
  \setstretch{1.44}%
  \ifcase \@ptsize \relax
    \setstretch {1.448}%
  \or
    \setstretch {1.399}%
  \or
    \setstretch {1.433}%
  \fi
}
\newcommand{\MSdoublespacing}{%
  \setstretch {1.92}%
  \ifcase \@ptsize \relax
    \setstretch {1.936}%
  \or
    \setstretch {1.866}%
  \or
    \setstretch {1.902}%
  \fi
}
\makeatother
\MSonehalfspacing

\setlength{\oddsidemargin}{0cm}
\setlength{\textwidth}{16cm}
\setlength{\parindent}{0cm}
\setlength{\parskip}{1.5ex plus0.5ex minus0.5ex}
